---

# Most settings should be configured in host_vars

downloads: ~/.ansible-downloads/

configure_dotfiles: false
configure_terminal: false
configure_osx: false

# Set to 'true' to configure the Dock via dockutil in host_vars
configure_dock: false


